1. Потрібно отримати файл з тарифами
2. Далі з аркуша "84030500 КомУслЭлектроэн" взяти колонки "МВЗ (Код)", "'Место возникновения затрат" та за відповідний місяць "kwt" і "uah".
3. Вставити ці дані у окремий ексель файл з назвою у форматі "tariff_yymm" (як шаблон можна дивитись на файл "tariff_2604")
4. Для перевірки виконай скрипт 
"""
select sum(consump_kwt), sum(consump_uah), avg(tariff) from Business_Analytic.prod.electro_tariff
where year=yyyy and month=mm
""" - сума має сходитись із вихідним файлом від колег
